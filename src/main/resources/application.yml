app:
  description: ${app.name} is a Spring Boot application
spring:
  jackson:
    property-naming-strategy: SNAKE_CASE
  output:
    ansi:
      enabled: ALWAYS
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://ec2-52-0-114-209.compute-1.amazonaws.com:5432/d9hhge33hqvhar
    username: ntrtxbftjrwbnp
    password: d2803d010d4ce6a272de804f83b7923e4d78ccc595924909dbfeae2b70d90da9
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    generate-ddl: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        time_zone: ${TIME_ZONE:UTC}
      org:
        hibernate:
          envers:
            default_schema: public
            store_data_at_delete: true
server:
  error:
    include-stacktrace: never
management:
  endpoints:
    web:
      exposure:
        include: "*"
    loggers:
      enabled: true